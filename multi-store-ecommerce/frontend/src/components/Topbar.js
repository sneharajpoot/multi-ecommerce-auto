import React from 'react';

const Topbar = () => {
  return (
    <div className="topbar">
      <h1>Dashboard</h1>
      {/* Add more topbar content as needed */}
    </div>
  );
};

export default Topbar;
